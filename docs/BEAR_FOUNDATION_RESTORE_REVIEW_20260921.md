# Bear foundation restoration and fresh historical replay

Status: local foundation repaired; **NOT CERTIFIED / DO NOT PROMOTE**.

## Superseding correction and task boundary

The v2 results below are retained evidence of the first recovered baseline,
not the current corrected mechanics. Broader tests exposed the old
with-replacement random shortcut and missing adaptive experimental function.
Recovered both from the later reviewed snapshot at
`bear_random_pool_mechanics_20260920_v2/implementation_snapshot/engine/utah_draw_predictive/bear.py`
(SHA-256 `916bce3e7653ee2bb3ee9a09f896c220c3cc05f46ba6d177661f945f1f61b63b`).
Current owner SHA-256: `f7353ff2ac01a99b26ec373c9c3897b9dc4e7d835dab74f835efab92e17d7006`.
Focused corrected checks: 128 passed. Broader suite: 476 passed / nine failures
in saved artifacts, coverage/registry counts and source-label expectations.
The corrected run is `bear_foundation_restore_20260921_v3`; no cap, probability
threshold or applicant-demand assumption was tuned to improve its result.

User redirected work to all-year/all-family scoring. That separate audit is
`audit_output_real_final/all_family_nine_fold_20260921_v1/`. It is not a Bear-only
result relabeled as full-family certification.

Corrected v3 replay completed. Its eight-fold hunting score is 9,078 rows,
12.062 pp MAE / 35.464 pp P90 / 13.384% tail; restricted pursuit is 308 rows,
15.458 pp MAE / 37.581 pp P90 / 22.078% tail. Both fail; zero unclassified gaps.
The separately labeled 2025→2026 diagnostic has 1,098 hunting rows / 13.550 pp
MAE and 60 pursuit rows / 18.379 pp MAE. The complete review retains 364
pre-ceiling false-certainty cases and 1,468 errors above 25 pp. All 20 protected
truth/runtime/registry files remain unchanged. Current authoritative evidence:
`audits/prediction_release_candidates/bear_foundation_restore_20260921_v3/review/`.
The remainder of this document describes the retained initial v2 comparison.

## What was repaired

The current Bear owner had lost reviewed ADR-0008 implementation: dated
canonical program classification, split-history boundaries, source-year-only
quota proxies, exact residency separation, duplicate/component checks and
source-only no-transition blanks. Existing tests could not even collect because
`BEAR_SPLIT_HISTORY_START` was missing. This was distinct from the previously
restored `validate_bear_availability_identity` import.

Restored the reviewed implementation from
`audits/prediction_release_candidates/bear_demand_forecast_20260920_v1/implementation_snapshot/engine/utah_draw_predictive/bear.py`,
verified against its candidate manifest SHA-256
`cbcbe3ee05b2b883bb04d909c015b8e577d8ff15971a50e3a20a419d6eb76e37`.
Preserved the newer strict availability identity, duplicate, numeric-zero and
blank-point checks. Restored the scorer's exact-code option without altering
its other probability precedence or thresholds. No alternate engine was built.
Archived opt-in cumulative modes are not selected by this replay.

This is recovery of a reviewed **failed baseline**, not a claim that the latest
statistical candidate was passing or that all experimental functions were recovered.
The active baseline uses `cohort_rollforward`, `simulation_mean`, 200 iterations,
seed 20260701, returning-cohort mode `off` and the existing 0.99 ceiling.

## Fresh results

Evidence root:
`audits/prediction_release_candidates/bear_foundation_restore_20260921_v2/`.
The ordinary historical acceptance window and diagnostic are separate directories.
Every forecast subprocess installs its source-read guard before model imports,
freezes forecasts before opening actuals, and retains source/code/scorer hashes.
All nine replays use the same hash-pinned ADR scorer and the existing final
website `mixed_row` entry point. Family and final probabilities agree exactly.
These are known retrospective years, **not newly unseen validation years**.

| Eight-fold design, 2017→2018 through 2024→2025 | Scored rows | MAE | P90 error | Tail above 25 pp | Unclassified gaps |
| --- | ---: | ---: | ---: | ---: | ---: |
| Limited-entry Bear hunting | 9,078 | 12.038 pp | 35.294 pp | 13.329% | 0 |
| Restricted Bear pursuit | 308 | 15.245 pp | 36.655 pp | 22.078% | 0 |

Both fail accuracy. Pursuit additionally fails the 400-row minimum. Adding empty
display rungs or duplicating rows is not a valid way to reach that minimum.
There are 643 source-classified hunting gaps and 23 pursuit gaps in this window.
Those are missing/blank forecasts, not numeric misses removed from scoring.

The separate 2025→2026 diagnostic has 1,098 hunting rows (MAE 13.495 pp) and
60 pursuit rows (MAE 18.258 pp). It contributes neither training nor acceptance
to the eight-fold result. The descriptive nine-fold totals are 10,176 hunting
rows and 368 pursuit rows; see the machine report for full metrics.

### The ceiling still masks certainty risk

Reported false guarantees are zero because this baseline retains the old 0.99
ceiling. The audit records **362 pre-ceiling false-certainty cases**, comprising
350 hunting and 12 restricted-pursuit cases; 321 are in the eight-fold window
and 41 in the diagnostic. These are not fixed by a probability cap.

Of these 362 cases, 27 have lower target-year awards. In 322 cases actual
applicants above the selected rung exceed forecast applicants above it. These
counts overlap: they are diagnostics, not exclusive causal classifications.
Do not change permit mechanics or use target applicants to alter the forecast.
The next statistical work must compare source-calibrated applicant-demand
behavior against this frozen baseline and retain every numeric error.

## Explicit audit outputs

`review_v3/` under the initial v2 root contains (`review_v2/` is a failed partial attempt):

- `bear_nine_fold_gate_summary.json`: separated windows and frozen thresholds.
- `forecast_freeze_inventory.json`: all nine forecast seals and source hashes.
- `bear_all_scored_diagnostics.csv`: 10,544 scored rows with source/target lanes,
  predecessor unsuccessful counts, quota provenance and parent/page references.
- `bear_high_error_rows.csv`: 1,459 errors above 25 pp, all retained in scoring.
- `bear_uncapped_false_certainty.csv`: 362 cases before the existing ceiling.
- `bear_actual_gaps.csv`: 795 classified gaps across both windows, zero unexplained.
- `bear_reasoned_blanks.csv`: 10,132 source-only no-transition display rows. This
  is **not** 10,132 positive-applicant scoring gaps; many are empty display rows.
- `bear_zero_forecast_positive_actual.csv`: zero rows, with the full schema.
- `protected_file_verification.json`: unchanged truth, DATABASE, registry and runtime.

Endpoint-backed 2026 records retain their source reference; no fictional PDF
page is invented. Source presence is not a new cell-level source certification.
The separate 3,886-row big-game LE audit and 32-row LE review were not rerun in
this Bear task; no Bear output is mislabeled as that work.

## Other requested checks

`deer_residency_balance.json` independently verifies all 105 ordinary general
deer hunts / 210 residency rows. Resident + nonresident equals regular-draw
allocation. DB1592 remains 97 + 10 = 107, separate from Planner total 200;
DB1630 remains 349 + 39 = 388, separate from Planner 720. No quota was overwritten.

The existing DATABASE feeder auditor passes with zero unexplained differences
against **its retained Planner/overlay inputs**, without repair. Its Planner
snapshot has 1,433 rows and is not the newer current-eligibility snapshot.
This does not itself approve the outstanding DATABASE raw-hash change or prove
current live equivalence. The machine-approved hash is deliberately unchanged.

Focused foundation/availability/final-preservation checks: 77 passed. Four new
review tests pass, including duplicate keys, separate residencies, multi-page
lineage and empty-output schema/overwrite protection. Public-manifest guard passes.
Broader test results are retained separately; they are not represented as all-pass.

## Reconciled request conflicts and remaining work

- `CODEX_INSTRUCTIONAL_V3_FINAL_FROM_WORKLOG.md` and
  `data/bear_fix_to_certified_plan.md` were not found. The actual availability
  and source-crosswalk reviews are under `docs/`, not the supplied paths.
- The requested `test_bear_program_source_boundary.py` and `gallery/assess_cert.py`
  are absent. The existing foundation/PDF-replay tests and accepted scorer were used.
- The initial broad experimental freeze helper failed collection because
  `_forecast_adaptive_cumulative_stack` is missing. Its failed attempt is retained
  at `bear_foundation_restore_20260921_v1/`. It is not the active baseline. The
  existing controlled baseline runner successfully completed all nine folds.
  The later recovery documented above resolves this missing-function failure.
- Did not use target applicants to choose a blank, remove quota-change numeric
  misses, force pursuit above 400 rows, or implement guessed retention/arrival rates.
- No duplicate `validate_bear_source_identity` implementation was added to the
  PDF crosswalk utility. Forecast admissibility stays in the existing Bear owner;
  post-score target comparison is a separate audit and cannot gate forecasting.
- Registry, certification limits, historical truth, DATABASE and normal runtime
  are unchanged. No stage, commit, push, R2 upload or deployment.

Remaining: experimental API/test drift, legacy saved-artifact failures,
evidence-backed demand calibration and passing unchanged statistical gates,
full DATABASE byte-delta authority review, then a separately authorized release.
