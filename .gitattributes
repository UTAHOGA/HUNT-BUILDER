# Binary/source-document handling only.
# Website runtime delivery must not use Git LFS; large public/runtime files are
# published to Cloudflare R2 and referenced through runtime manifests.
*.pdf binary
*.xlsx binary
*.xls binary

# Preserve raw SHA-256 bytes for the frozen restricted-pursuit policy.
docs/decisions/ADR-0009-restricted-pursuit-limited-evidence-policy.md text eol=lf
governance/bear-restricted-pursuit-limited-evidence.v1.json text eol=lf
