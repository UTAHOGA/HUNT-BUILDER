# AGENTS.md

Read this file before every task. Tyler's explicit instruction controls the task scope: if Tyler tells you to edit website, deploy, UI, production, engine, data, or truth files, then that file class is in scope for that task. Do not add extra restrictions that block the requested work. Before starting, check git status --short and git diff --cached --name-status; if anything is unexpectedly staged, stop and ask. Do not stage or commit large generated data files: files over 100 MB are prohibited from Git, files over 50 MB require review, and large generated JSON/CSV/GeoJSON/SQLite/runtime outputs belong in Cloudflare R2, not Git. Do not use Git LFS unless Tyler explicitly requests it. Never upload or commit .git, .wrangler/state, 
ode_modules, cache blobs, or local generated audit junk. Vercel deploys from pages-dist because ercel.json uses uildCommand: npm run build and outputDirectory: pages-dist; do not delete pages-dist. pages-dist may be edited or rebuilt when Tyler asks for website/deploy/build/live work. For data-engine/audit/model work, avoid unrelated website/deploy changes unless Tyler asks for them. For website/deploy/build/live work, edit the necessary website files directly and rebuild pages-dist as required. scripts/build-pages-dist.js should avoid duplicating huge runtime data into pages-dist; files over 100 MB should be R2-backed or skipped with a small manifest when possible. Prediction-engine work should follow the simple loop: feed official scorable truth rows, generate predictions, compare against following-year official actual truth, identify error patterns, edit engine logic, and rerun. Ignore unscorable rows for accuracy work and never fabricate probability from permit totals, quota rows, CWMU contact-operator rows, guidebook rows, allocation-only rows, or reference rows. Sportsman permits are SPORTSMAN_RANDOM_ONLY; youth rows must be source-classified and not guessed; guaranteed/lifetime/general guaranteed-tag rows are not prediction rows; CWMU Contact Operator rows with blank permit numbers are CWMU_CONTACT_OPERATOR_REFERENCE_ONLY; allocation/reference-only rows are not probability rows. Before finishing any task, run git diff --cached --name-status and git status --short, report whether anything is staged, and do not leave large generated files staged.
